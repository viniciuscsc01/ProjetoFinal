@model IEnumerable<ProjetoFinal.Models.LocalRealizacao>

@{
    ViewData["Title"] = "Index";
}

<h1>LocalRealizacao</h1>

<p>
    <a asp-action="Create">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocalNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocalNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cidade.CidadeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LocalRealizacaoId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.LocalRealizacaoId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.LocalRealizacaoId">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
