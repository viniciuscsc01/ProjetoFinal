@model IEnumerable<ProjetoFinal.Models.TipoColaborador>

@{
    ViewData["Title"] = "Index";
}

<h1>TipoColaborador</h1>

<p>
    <a asp-action="Create">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoColaboradorNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoColaboradorNome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoColaboradorId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.TipoColaboradorId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoColaboradorId">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
