@model IEnumerable<ProjetoFinal.Models.Procedimento>

@{
    ViewData["Title"] = "Index";
}

<h1>Procedimento</h1>

<p>
    <a asp-action="Create">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProcedimentoNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProcedimentoObservacaoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoProcedimento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProcedimentoNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProcedimentoObservacaoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoProcedimento.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProcedimentoId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ProcedimentoId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.ProcedimentoId">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
